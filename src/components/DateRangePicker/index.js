import ReDateRangePicker from './src/DateRangePicker.vue';

/* istanbul ignore next */
ReDateRangePicker.install = (Vue) => {
  Vue.component(ReDateRangePicker.name, ReDateRangePicker);
};

export default ReDateRangePicker;
