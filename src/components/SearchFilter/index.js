import ReSearchFilter from './src/SearchFilter.vue';

/* istanbul ignore next */
ReSearchFilter.install = (Vue) => {
  Vue.component(ReSearchFilter.name, ReSearchFilter);
};

export default ReSearchFilter;
