import ReEmailAutoComplete from './src/EmailAutoComplete.vue';

/* istanbul ignore next */
ReEmailAutoComplete.install = (Vue) => {
  Vue.component(ReEmailAutoComplete.name, ReEmailAutoComplete);
};

export default ReEmailAutoComplete;
