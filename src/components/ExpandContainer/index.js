import ReExpandContainer from './src/ExpandContainer.vue';

/* istanbul ignore next */
ReExpandContainer.install = (Vue) => {
  Vue.component(ReExpandContainer.name, ReExpandContainer);
};

export default ReExpandContainer;
