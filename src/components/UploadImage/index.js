import ReUploadImage from './src/UploadImage.vue';

/* istanbul ignore next */
ReUploadImage.install = (Vue) => {
  Vue.component(ReUploadImage.name, ReUploadImage);
};

export default ReUploadImage;
