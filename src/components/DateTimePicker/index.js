import ReDateTimePicker from './src/DateTimePicker.vue';

/* istanbul ignore next */
ReDateTimePicker.install = (Vue) => {
  Vue.component(ReDateTimePicker.name, ReDateTimePicker);
};

export default ReDateTimePicker;
