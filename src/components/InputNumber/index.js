import ReInputNumber from './src/InputNumber.vue';

/* istanbul ignore next */
ReInputNumber.install = (Vue) => {
  Vue.component(ReInputNumber.name, ReInputNumber);
};

export default ReInputNumber;
